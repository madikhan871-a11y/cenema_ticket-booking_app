import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF09090D);
  static const surface = Color(0xFF15151C);
  static const surfaceLight = Color(0xFF20202A);

  static const primary = Color(0xFFE50914);
  static const primaryDark = Color(0xFF9B0008);

  static const purple = Color(0xFF7B4DFF);
  static const gold = Color(0xFFFFC857);

  static const white = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFF9999A5);

  // Light text used on the ticket card.
  static const textDark = Color(0xFF17171D);

  static const border = Color(0xFF292934);

  static const availableSeat = Color(0xFF777783);
  static const selectedSeat = Color(0xFFE50914);
  static const occupiedSeat = Color(0xFF292934);
}
